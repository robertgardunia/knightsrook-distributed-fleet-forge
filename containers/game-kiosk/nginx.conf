server {
    listen 8080;
    root /usr/share/nginx/html;

    # Default to attract title screen
    location = / {
        return 301 /title.html;
    }

    location / {
        try_files $uri $uri/ =404;
    }

    # Disable access log noise
    access_log off;
    error_log /dev/null crit;
}
