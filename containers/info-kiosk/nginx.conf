server {
    listen 8080;
    root /usr/share/nginx/html;

    location = / {
        return 301 /slideshow.html;
    }

    location / {
        try_files $uri $uri/ =404;
    }

    access_log off;
    error_log /dev/null crit;
}
